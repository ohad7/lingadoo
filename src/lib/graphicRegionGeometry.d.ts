export function sourceGraphicBboxToEditor(
  pageWidth: number,
  bbox: number[],
  mirrorEnabled: boolean,
): number[] | null;

export function editorGraphicBboxToSource(
  pageWidth: number,
  bbox: number[],
  mirrorEnabled: boolean,
): number[] | null;
